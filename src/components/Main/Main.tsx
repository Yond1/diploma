import { CatalogPage } from 'pages'
import { Route, Routes } from 'react-router'
import { MainRoute } from 'routes'

export const Main = () => {
    return (
        <main>
            <MainRoute />
        </main>
    )
}
